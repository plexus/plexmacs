(setq plexus/record-counter "**")
(setq plexus/record-video nil)
(setq plexus/record-audio nil)
(setq plexus/record-state nil)
(setq plexus/record-clipped nil)

;; (add-hook 'find-file-hook #'plexus/screencast-mode)
